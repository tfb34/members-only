<h1>Log in</h1>

<%= form_for :user do |f| %>
	<%= f.label :email%>
	<%= f.email_field :email %><br>

	<%= f.label :password%>
	<%= f.password_field :password %><br>

	<%= f.label :remember_me %>
	<%= f.check_box :remember_me%>

	<%= f.submit "Log in"%>
<% end %>
